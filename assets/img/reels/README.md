# Reel covers

Optional. Drop a cover here named after the reel id in `assets/js/data.js`:

    reel-1.jpg   reel-2.jpg   reel-3.jpg   reel-4.jpg

Shot vertically (9:16). If a file is missing the tile falls back to a branded
texture, so the grid never looks broken.
